<div class="container-fluid">
  <div class="row-fluid">
    <div class="span12">
      {{#each post in model}}
        {{render "postItem" post}}
      {{/each}}
    </div>
  </div>
</div>

{{outlet}}


